<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.BoardAttachMapper">

	<insert id="insert">
		insert into ATTACH_TABLE (b_uuid, b_uploadpath, b_filename, b_number)
		values (#{b_uuid}, #{b_uploadPath}, #{b_fileName}, #{b_number} )
	</insert>

	<delete id="delete">

		delete from ATTACH_TABLE where b_uuid = #{b_uuid}

	</delete>

	<select id="findByB_number"
		resultType="org.zerock.domain.BoardAttachVO">
		select * from ATTACH_TABLE where b_number = #{b_number}
	</select>

	<delete id="deleteAll">

		delete ATTACH_TABLE where b_number = #{b_number}

	</delete>

	<select id="getOldFiles"
		resultType="org.zerock.domain.BoardAttachVO">

		select * from ATTACH_TABLE where b_uploadpath = to_char(sysdate -1 ,'yyyy\mm\dd')

	</select>
</mapper>
